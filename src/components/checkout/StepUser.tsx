import { Step } from "@/types/Step";

type Props = {
    setStep:(step:Step) => void;
}

export const StepUser = ({setStep}:Props) => {
    return (
        <div>
            ...
        </div>
    )
}