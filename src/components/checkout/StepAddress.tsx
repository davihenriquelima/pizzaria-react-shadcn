import { Step } from "@/types/Step";

type Props = {
    setStep:(step:Step) => void;
}

export const StepAddress = ({setStep}:Props) => {
    return (
        <div>
            ...
        </div>
    )
}